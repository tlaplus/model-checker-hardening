---
state: closed
labels: [tlc]
---

# An evaluation error in an eventuality of the initial state exits with status 0

## Summary

Fixed in https://github.com/tlaplus/tlaplus/pull/1440

When a temporal property's eventuality (`<>P`, or either side of `~>`) raises
an evaluation error in the initial state, TLC prints the error and then ends model
checking with exit status 0. `TLC.process()` returns 0 as well. The run stops
with states left on the queue, and no property is checked. A consumer that reads
the exit status records a negative specification as a successful check.

The status also hides an invariant violation. When the initial state violates the
invariant, TLC prints `Invariant Inv is violated by the initial state` after the
evaluation error and still exits 0, so a counterexample TLC itself reported is
recorded as a pass.

Observed with tla2tools 1.8.0-SNAPSHOT (Maven snapshot
`1.8.0-20260912.025952-68`). The same error elsewhere is reported with an
evaluation-failure status: exit 75 in the initial predicate or under `[]`, and
exit 76 when a `[]` property fails in a later state.

## Reproduction

`FuzzInput.tla`:

```tla
---- MODULE FuzzInput ----
EXTENDS Integers, Sequences
VARIABLE x
Init == x = 0
Next == UNCHANGED x
Spec == Init /\ [][Next]_x
Prop == <>(Head(<<>>) = x)
====
```

`FuzzInput.cfg`:

```text
SPECIFICATION Spec
PROPERTY Prop
```

```sh
java -cp tla2tools.jar tlc2.TLC -deadlock -noGenerateSpecTE -config FuzzInput FuzzInput
echo $?
```

```text
Implied-temporal checking--satisfiability problem has 1 branches.
Computing initial states...
Error: Attempted to apply Head to the empty sequence.
1 states generated, 1 distinct states found, 1 states left on queue.
The depth of the complete state graph search is 1.
Finished in 00s at (2026-09-14 12:03:22)
0
```

Calling `new TLC()`, `handleParameters` with the same arguments and `process()`
returns 0, which `EC.ExitStatus.errorConstantToExitStatus` maps to 0.

The status depends on the temporal operator around the same failing predicate:

| `Prop` | Exit status |
| --- | ---: |
| `<>(Head(<<>>) = x)` | 0 |
| `(x = 0) ~> (Head(<<>>) = x)` | 0 |
| `<>(x \div 0 = 1)` | 0 |
| `<>((CHOOSE k \in {} : TRUE) = x)` | 0 |
| `((x \div 0) = 1) ~> FALSE` | 0 |
| `~[](Head(<<>>) = x)` | 0 |
| `([](Head(<<>>) = x)) => FALSE` | 0 |
| `lab :: [](Head(<<>>) = x)` | 0 |
| `[](lab :: (Head(<<>>) = x))` | 75 |
| `[](Head(<<>>) = x)` | 75 |
| `[][] (Head(<<>>) = x)` | 0 |
| `[][][] (Head(<<>>) = x)` | 0 |
| `[][] (x \div 0 = 1)` | 0 |

A negated `[]` is an eventuality, and so is a `[]` on the left of an implication.
A label directly on `[]` also exits 0, although the unlabeled formula and a label
inside `[]` exit 75. A `[]` under another `[]` exits 0 as well, at any depth, so
the exit-75 case is only a bare single `[]`.

### A masked invariant violation

Adding `Inv == x # 0`, which the initial state violates, with `INVARIANT Inv` in the
configuration:

| `Prop` | TLC prints | Exit status |
| --- | --- | ---: |
| `TRUE` | `Invariant Inv is violated by the initial state` | 12 |
| `<>((x \div 0) = 1)` | `The second argument of \div is 0`, then `Invariant Inv is violated by the initial state` | 0 |
| `((x \div 0) = 1) ~> FALSE` | the same two errors | 0 |
| `(x = 0) ~> ((x \div 0) = 1)` | the same two errors | 0 |
| `~[]((x \div 0) = 1)` | the same two errors | 0 |
| `lab :: []((x \div 0) = 1)` | the same two errors | 0 |
| `[]((x \div 0) = 1)` | `Invariant Inv is violated by the initial state` | 12 |

The corpus20 input
[`016a5be5...`](../../corpus20/03aggregator-fail/016a5be5d02d62185477a16f604f0bfed50cdb0dff0eb843617a8226de78025b.cbor)
has this shape: its property is `P ~> FALSE`, where `P` reaches `% 0`. TLC printed
both errors and was recorded as a pass, while Apalache reported the invariant
violation. The run used TLC2 version 2026.09.12.025210 (rev `867aefb`).

### Recurring in corpus22

The `module` corpus22 has 80 aggregator deviations where TLC passes and Apalache
reports a counterexample. Rerunning TLC on all 80 with `SPECIFICATION Spec`,
`INVARIANT Inv` and `PROPERTY Prop`, 78 print an evaluation error and exit 0. The
errors are a function applied outside its domain (18), an out-of-bounds tuple
index (18), `CHOOSE` without a witness (12), `Head` of the empty sequence (12), a
`CASE` without a matching arm (8), `SubSeq` outside the domain (5), `Tail` of the
empty sequence (2), `0^0` (2) and `\div` by zero (1). Each of the 78 properties
contains `<>` or `~>`. The other two deviations are
[`CHOOSE` with more than one witness](../../conformance/choose-multiple-witnesses.md).

### Recurring in corpus23

The `module` corpus23 has 69 aggregator deviations where TLC passes and Apalache
reports a counterexample. Rerunning TLC on all 69 as above, every one prints an
evaluation error in the initial state and exits 0. The errors are a function
applied outside its domain (17), `CHOOSE` without a witness or another "value of
an expression of form" error (16), an out-of-bounds tuple index (14), a `CASE`
without a matching arm (10), `Head` of the empty sequence (9), `SubSeq` outside
the domain (1), `\div` by zero (1) and `0^0` (1). 66 properties contain `<>` or
`~>`. Of the other three, `40642b46` is `~[](...)`, `70bab3ff` is `[](...) => FALSE`,
and `23d460f0` is a labeled `[](...)`; the last two also violate `Inv` in the
initial state.

### Recurring in corpus25

The `module` corpus25, generated like corpus24 with twice its entry budget, has
105 aggregator deviations where TLC passes and Apalache reports a
counterexample. Rerunning TLC on all 105 as above, every one prints an
evaluation error and exits 0, and 72 of them also print
`Invariant Inv is violated by the initial state` and still exit 0. This is every
deviation in that verdict direction: the run has no TLC-pass deviation with
another cause.

The errors are a `CASE` without a matching arm (24), an out-of-bounds tuple
index (22), `CHOOSE` without a witness or another "value of an expression of
form" error (19), a function applied outside its domain (18), `Head` of the
empty sequence (13), `SubSeq` outside the domain (2), `Tail` of the empty
sequence (2), `0^0` (2), a nonpositive `%` divisor (2) and `\div` by zero (1).
104 properties contain `<>` or `~>`; the remaining one, `ddbc8673`, is
`[](P) => FALSE`, the implication row of the table above.

Run with TLC2 version 2026.09.16.053301.

### Recurring in corpus26

The `module` corpus26, generated like corpus25 with half its entry budget, has
60 aggregator deviations where TLC passes and Apalache reports a counterexample.
Rerunning TLC on all 60 as above, every one prints an evaluation error and exits
0, and 43 of them also print `Invariant Inv is violated by the initial state`.
As in corpus25, this is every deviation in that verdict direction. Apalache
0.62.2, rerun on each entry's IR with the workflow's arguments, reports the
counterexample (exit 12) for all 60.

The errors are a function applied outside its domain (23), an out-of-bounds
tuple index (10), `Head` of the empty sequence (8), a `CASE` without a matching
arm (7), `CHOOSE` without a witness or another "value of an expression of form"
error (5), `0^0` (3), `\div` by zero (2), `Tail` of the empty sequence (1) and a
nonpositive `%` divisor (1). 59 properties contain `<>` or `~>`; the remaining
one, `b7956b02`, is `([](P) => FALSE) => v`, the implication row of the table
above.

### Recurring in corpus28

The `module` corpus28 run adds generational mutation
([ADR 0010](../../docs/decisions/0010-mutation.md)) to the corpus26 categories.
It has 46 aggregator deviations where TLC passes and Apalache reports a
counterexample. Rerunning TLC on all 46 as above, 45 print an evaluation error
while computing the initial states and exit 0, and 32 of those also print
`Invariant Inv is violated by the initial state`. Every one of the 45 properties
contains `<>` or `~>`. Apalache 0.62.2, rerun on each entry's IR with the
workflow's arguments, reports the counterexample (exit 12) for all 45: in state
0 for the 32, and in state 1 or 2 for the other 13, which TLC never reaches. The
remaining deviation, `c1a07b70`, has no evaluation error; it is
[`CHOOSE` with more than one witness](../../conformance/choose-multiple-witnesses.md).

The errors are a `CASE` without a matching arm (14), `CHOOSE` without a witness
or another "value of an expression of form" error (13), an out-of-bounds tuple
index (8), a function applied outside its domain (5), `Head` of the empty
sequence (4) and `Tail` of the empty sequence (1). Seven of the 45 are mutants:
five `splice` (`7edafc28`, `87603871`, `a27d85fe`, `ad00ce41`, `d1d45fb5`), one
`duplicate` (`16f6fe62`) and one `copy` (`fcfeda0a`).

Run with TLC commit `957faa0` (tla2tools `1.8.0-20260916.164343-73`) and
FuzzTLA `41bda26`.

### Recurring in corpus32

The `module` corpus32 run has one aggregator deviation that reruns to this
defect. `88e75b3b` was recorded as TLC-pass/Apalache-counterexample; its
property is `[][] ((CASE F -> step) < 1)`, where the combinator of a nested
`ApaFoldSet` makes the guard `F` the constant `FALSE`. Rerunning TLC with
`SPECIFICATION Spec`, `INVARIANT Inv` and `PROPERTY Prop` prints
`Attempted to evaluate a CASE with no conditions true.` while computing the
initial states and exits 0; the `[][]` prefix is the new table row above.
Without the property, TLC finds the same invariant violation Apalache reports
(`var0 >= step` fails at step 2) and exits 12. Re-running the input through the
workflow reproduces the recorded verdicts: TLC passes with one state and zero
fired actions, Apalache 0.62.2 reports `State 2: state invariant 0 violated`.

Run with TLC commit `142d0ba` (tla2tools `1.8.0-20260917.033119-76`), Apalache
0.62.2 (build `f0dec98`) and FuzzTLA `fe50088`.

### An eventuality failing in a later state

When the eventuality fails only in a later state, as in
`<>(x = 1 /\ Head(<<>>) = x)` with `Next == (x < 2 /\ x' = x + 1) \/ UNCHANGED x`,
TLC reports the behavior and exits 255; that status is the module-constant
mapping described in [tlc-002](tlc-002.md).

## Expected behavior

TLC reports the evaluation error with a failure exit status, as it does for the
same predicate under `[]` (exit 75).

## Impact

Automated workflows that classify TLC runs by exit status, including this
repository's conformance aggregator, record the specification as passing, even
when TLC also found an invariant violation. The TLC-versus-Apalache comparison
then attributes the difference to Apalache.
The fuzzing workflow generates such properties once the `temporal` generator
category is enabled.
